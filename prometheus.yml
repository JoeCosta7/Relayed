global:
  scrape_interval: 15s  # Time between metric pulls

scrape_configs:
  - job_name: 'relayed-metrics'
    metrics_path: '/metrics'
    static_configs:
      - targets: ['app:8000', 'worker:8001']  